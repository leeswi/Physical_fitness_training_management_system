设计表格，表格名称，表内字段，主键外键

分工

例如：plan（训练计划表）：id
                        inputid
                        inputdate
                        userid
                        companyid
                        subject
                        status
                        startdate
                        enddate
                        day
                        begintime
                        finishtime
                        place
                        level
                        teamid
                        departmentid
                        del_userid
                        del_status



其他表：
例如：
ptime(训练时间表)：  tno(编号[主键])、
						starttime(可用来训练的时间段)
						endtime
pplace：plid
        place

pcontent：  cid
            content
            level
剩余表：
杨傲：
content(训练内容)：
place(训练场地)：
郭永旭：
user(用户表)：
team(分组表)：
谢树海：
score(训练成绩表)：

要求：
1、把这些表格字段设计出来，像举例的那样
2、标明主键外键
3、联系实际想想，别糊弄

